# Conductor

The conductor is Cowork or Codex running in dialogic mode: an agentic collaborator that reads the repository, sequences work with the human, and keeps decisions inspectable instead of silently converting intent into output.

Conductor flows sequence recipes, enforce phase gates, and require human clearance before proceeding. Silent mode is only available for flows in `conductor/verified/` that have earned that status through documented dialogic runs.
